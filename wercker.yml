box: ruby:2.2.2
build:
    steps:
        - bundle-install
        - script:
            name: test integrated
            code: bundle exec rake test
        - script:
            name: test isolated
            code: bundle exec rake test:isolated
